// import '@babel/polyfill'
import './index.css';

function add(x,y) {
  return x+y;
}

const mut = (x, y) => {
  return x - y
}

// eslint-disable-next-line
console.log(mut(1, 2));
console.log(add(1,2),data);